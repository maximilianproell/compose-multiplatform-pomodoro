CREATE TABLE IF NOT EXISTS WorkPackageTable (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    startDate TEXT NOT NULL,
    minutes INTEGER NOT NULL
);

insert:
INSERT INTO WorkPackageTable(id, startDate, minutes)
VALUES (?,?,?);